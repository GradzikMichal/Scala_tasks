/*

1. For each university, estimate the average number of students in a course: The key attributes should be `university` and `courseID`.
2. Estimate the fraction of students who have a Grade Point Average of 3.5 or more: The key attribute should be `university` and `studentID`.
3. Estimate the fraction of courses where at least half the students got “A”: The key attributes should be `university` and `courseID`.

 */